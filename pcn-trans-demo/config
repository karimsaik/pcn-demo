// Idle time, in ms, between two trips. I.e. if the time elapsed between the current event and the last event 
// is >= idle_duration, we consider that a new trip has started
var idle_duration = 25000; //15 * 60000; 